version: "3.8"

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    env_file: .env
    volumes:
      - ./uploads:/app/uploads

  web:
    build: ./web
    ports:
      - "5173:80"
    depends_on:
      - api
